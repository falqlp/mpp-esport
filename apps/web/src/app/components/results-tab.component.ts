import { DatePipe } from '@angular/common'; import { Component, Input } from '@angular/core'; import { FormControl, ReactiveFormsModule } from '@angular/forms'; import { MatCardModule } from '@angular/material/card'; import { MatFormFieldModule } from '@angular/material/form-field'; import { MatSelectModule } from '@angular/material/select';
import { CompetitionKey } from '../../services/auth.service'; import { LolMatch } from '../../services/esports-api.service'; import { COMPETITIONS, filterMatches } from '../competition.utils';
@Component({selector:'app-results-tab',standalone:true,imports:[DatePipe,ReactiveFormsModule,MatCardModule,MatFormFieldModule,MatSelectModule],template:`
<section class="grid"><mat-card appearance="outlined" class="filter"><mat-form-field appearance="outline" subscriptSizing="dynamic"><mat-label>Compétition</mat-label><mat-select [formControl]="filter"><mat-option value="favorites">Mes compétitions suivies</mat-option>@for(c of competitions;track c.value){<mat-option [value]="c.value">{{c.label}}</mat-option>}</mat-select></mat-form-field></mat-card>
<mat-card appearance="outlined" class="list"><mat-card-header><mat-card-subtitle>Résultats</mat-card-subtitle><mat-card-title>Matchs terminés</mat-card-title></mat-card-header><mat-card-content class="matches">@for(match of filtered();track match.id){<button class="row" type="button" (click)="selectedId=match.id"><span><strong class="competition">@if(match.leagueLogoUrl){<img [src]="match.leagueLogoUrl" [alt]="match.league">}{{match.league}}</strong><small>{{match.tournament}} · {{match.format}}</small></span><b class="result-teams">@if(match.teams[0].logoUrl){<img [src]="match.teams[0].logoUrl" [alt]="match.teams[0].name">}{{match.teams[0].code}} {{match.result?.score?.[0]}} – {{match.result?.score?.[1]}} {{match.teams[1].code}}@if(match.teams[1].logoUrl){<img [src]="match.teams[1].logoUrl" [alt]="match.teams[1].name">}</b><time>{{match.startsAt|date:'EEE d MMM, HH:mm'}}</time></button>}@empty{<p>Aucun résultat pour les compétitions suivies.</p>}</mat-card-content></mat-card>
<mat-card appearance="outlined"><mat-card-header><mat-card-subtitle>Feuille de match</mat-card-subtitle><mat-card-title>Détail du résultat</mat-card-title></mat-card-header><mat-card-content>@if(selected();as match){<div class="detail"><small>{{match.league}} · {{match.tournament}}</small><div class="score"><span><strong>{{match.teams[0].name}}</strong><small>{{match.teams[0].code}}</small></span><b>{{match.result?.score?.[0]}} – {{match.result?.score?.[1]}}</b><span><strong>{{match.teams[1].name}}</strong><small>{{match.teams[1].code}}</small></span></div><p>Vainqueur : <strong>{{teamName(match,match.result?.winnerId??'')}}</strong></p></div>}@else{<p>Sélectionne un match pour afficher son résultat.</p>}</mat-card-content></mat-card></section>`,styles:[`
.grid{display:grid;gap:16px;grid-template-columns:2fr 1fr}.filter{grid-column:1/-1;display:flex;justify-content:flex-end;padding:12px 16px}.filter mat-form-field{max-width:420px;width:100%}mat-card-header{margin-bottom:16px}.matches{display:grid;gap:10px;max-height:540px;overflow:auto}.row{align-items:center;background:var(--mat-sys-surface-container-low);border:1px solid var(--mat-sys-outline-variant);border-radius:14px;color:inherit;cursor:pointer;display:grid;gap:16px;grid-template-columns:1fr 1fr auto;font:inherit;padding:14px;text-align:left;width:100%}.row>span,.score span{display:grid}.competition,.result-teams{align-items:center;display:flex;gap:8px}.competition img{height:24px;object-fit:contain;width:24px}.result-teams{justify-content:center}.result-teams img{height:34px;object-fit:contain;width:40px}.row small,time,.detail>small,.score small{color:var(--mat-sys-on-surface-variant)}.detail{display:grid;gap:24px}.score{align-items:center;display:grid;gap:12px;grid-template-columns:1fr auto 1fr;text-align:center}.score>b{color:var(--mat-sys-primary);font-size:28px}.detail p{background:var(--mat-sys-secondary-container);border-radius:12px;padding:14px;text-align:center}@media(max-width:980px){.grid{grid-template-columns:1fr}.filter{grid-column:1}.score{grid-template-columns:1fr}}`],})
export class ResultsTabComponent{@Input()matches:LolMatch[]=[];@Input()favorites:CompetitionKey[]=[];readonly competitions=COMPETITIONS;readonly filter=new FormControl('favorites',{nonNullable:true});selectedId='';filtered(){return filterMatches(this.matches,this.filter.value,this.favorites).sort((a,b)=>b.startsAt.localeCompare(a.startsAt));}selected(){return this.matches.find(x=>x.id===this.selectedId)??this.filtered()[0];}teamName(match:LolMatch,id:string){return match.teams.find(x=>x.id===id)?.name??id;}}
